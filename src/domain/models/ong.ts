export type OngModel = {
  id: string
  name: string
  email: string
  whatsapp: string
  city: string
  uf: string
}

export type OngType = {
  name: string
  email: string
  whatsapp: string
  city: string
  uf: string
}
